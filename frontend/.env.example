# Frontend env (Vite). Copy to `.env` to override defaults.
# Leave unset for local dev — requests proxy to http://localhost:4000 via vite.config.ts.
# For a deployed frontend, set this to your deployed API origin, e.g. https://api.example.com
VITE_API_BASE_URL=
